node_modules/
dist/
.astro/
*.tsbuildinfo
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
.pytest_cache/

apps/docs/node_modules/

# Rust
packages/clients/rust/target/
packages/cli/target/

# Misc
.worktrees/
.rumdl_cache/

# Upstream reference checkouts (local only, not for commit)
/.upstream/

# Onboarding scratch (temp, not for commit)
/onboarding/

# Project-level MCP servers (local only, not for commit)
/.mcp.json

# Agent tooling (local only, not for commit)
/.claude/
/.agents/
