---
title: Second Nested Page
description: Confirms ordering inside a nested folder.
icon: FileText
---

This page is listed after `first` in the folder's `meta.json`, so it must appear
second regardless of filesystem order.
