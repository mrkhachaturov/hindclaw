---
import { cn } from '@/lib/utils';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={cn('grid grid-cols-2 gap-3 @container', className)}>
  <slot />
</div>
